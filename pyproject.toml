[project]
name = "kurs-plan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.5",
    "webtool-template",
]

[tool.uv.sources]
webtool-template = { path = "webtool_template", editable = true }
